<!-- Home page with two different states: 'landing' and 'logged in' -->
<div class="landing-wrapper">
  <% if user_signed_in? %>
    <%= render 'shared/home_logged_in' %>
  <% else %>
    <%= render 'shared/landing' %>
  <% end %>
</div>
